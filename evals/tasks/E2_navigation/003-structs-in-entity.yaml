id: E2_003
kind: symbols_in_file
pinned_ref: 81db5bd
question: |
  List every top-level struct defined in `src/entity.rs`. Include only
  `struct` declarations at the file's top scope — exclude enums, traits,
  type aliases, and anything nested inside an `impl`, `mod`, or test
  module. Return ONLY a JSON array of struct names, sorted lexicographically.
grader:
  type: set_match
  expected:
    - BlastRadius
    - Entity
    - Reference
